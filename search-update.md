请帮我优化冰盒（Android Kotlin Compose + Room 项目）的搜索功能。

当前情况：
- 样本数据已有 date、name 等字段。
- 当前搜索已经支持大小写不敏感。
- 不希望修改数据库结构，不新增字段，不做 Room migration。
- 搜索仍然保持一个搜索框，默认使用增强模糊搜索，不增加“精确/智能”切换。

目标：
实现一个统一的 SearchNormalizer，让搜索更加符合实验室样本管理场景。

需要支持：

1. 样本名称模糊搜索

目前：
- R100 和 R-100 会被认为是不同内容。

希望：
搜索时忽略以下差异：
- 大小写
- 连字符 -
- 下划线 _
- 空格
- 点号 .

例如：

搜索：
R100

可以匹配：
R100
R-100
R_100
R 100
r.100

但不要做过度匹配，例如不要简单删除所有字符导致 R100 和 R10-0 等完全不同编号混淆。


2. 日期智能搜索

当前日期字段可能来自：
- 日期选择器
- OCR 自动识别
- 用户手动输入

存在格式不统一：

例如：
2026-07-11
20260711
260711
202607
0411
04-11

希望搜索时自动识别日期格式，并生成候选匹配。

例如：

搜索：
260711

应该可以匹配：
260711
2026-07-11
2026.07.11
20260711


搜索：
202607

应该可以匹配：
2026-07
2026/07
202607


搜索：
0411

应该可以匹配：
0411
04-11
04.11
根据当前年份合理推断：
2026-04-11

注意：
- 不要修改用户原始日期数据。
- 只在搜索阶段进行解析。
- 对缺少年份或日期的情况不要强行写回数据库。


3. 搜索架构要求

请先分析当前项目搜索实现，然后给出最小改动方案。

建议结构：

用户输入 keyword
        ↓
SearchNormalizer
        ↓
生成多个 normalized keyword
        ↓
调用现有 repository / DAO 查询
        ↓
返回结果


要求：
- 尽量复用当前 Room 查询逻辑。
- 不要为了这个功能增加数据库字段。
- 不影响现有导入导出功能。
- 不影响旧版本数据兼容。
- 保持 Kotlin 代码风格。


请输出：
1. 当前代码需要修改的位置分析。
2. 推荐实现方案。
3. SearchNormalizer 的完整 Kotlin 实现。
4. Repository/DAO/ViewModel 需要如何接入。
5. 说明可能存在的误匹配风险以及如何避免。