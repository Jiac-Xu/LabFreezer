# LabFreezer 系统设计文档

## 1. 产品架构设计

### 整体架构：MVVM + Repository 模式

```
UI Layer (Compose + Miuix)
    ↕ ViewModel
Domain Layer (Repository)
    ↕ 
Data Layer (Room DAO + File Storage)
```

### 技术选型
| 层面 | 技术 |
|------|------|
| UI | Jetpack Compose + Miuix |
| 导航 | Compose Navigation（单 Activity） |
| 架构 | MVVM (ViewModel + StateFlow) |
| 数据库 | Room |
| 相机 | CameraX + 系统相机 Intent |
| 图片加载 | Coil |
| 依赖注入 | Hilt |
| 异步 | Kotlin Coroutines + Flow |
| 导出 | Apache POI (xlsx) + 手写 Markdown |

### 模块包结构
```
com.labfreezer/
├── data/
│   ├── db/           — Room 数据库 (DB, DAO, Entity)
│   ├── repository/   — Repository 实现
│   └── file/         — 文件/照片存储管理
├── domain/
│   ├── model/        — 业务模型
│   └── repository/   — Repository 接口
├── ui/
│   ├── theme/        — Miuix 主题配置
│   ├── navigation/   — 导航图
│   ├── devices/      — 设备列表
│   ├── layers/       — 层列表
│   ├── boxgrid/      — 冻存盒网格
│   ├── sample/       — 样本详情/编辑
│   ├── camera/       — 相机拍照
│   ├── search/       — 搜索
│   └── export/       — 导出
├── camera/           — CameraX 封装
├── export/           — 导出引擎 (xlsx/md)
└── di/               — Hilt 模块
```

---

## 2. 数据库表结构

```sql
-- 存储设备（-80A、-80B、液氮1...）
CREATE TABLE storage_device (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    name        TEXT NOT NULL,          -- 设备名称
    type        TEXT NOT NULL DEFAULT 'FREEZER_M80',  -- FREEZER_M80 | LIQUID_NITROGEN
    note        TEXT,
    sort_order  INTEGER NOT NULL DEFAULT 0,
    created_at  INTEGER NOT NULL,       -- epoch millis
    updated_at  INTEGER NOT NULL
);

-- 存储层（设备下的每一层）
CREATE TABLE storage_layer (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    device_id   INTEGER NOT NULL REFERENCES storage_device(id) ON DELETE CASCADE,
    name        TEXT NOT NULL,          -- 层名称（如"第1层"、"上层"）
    note        TEXT,
    sort_order  INTEGER NOT NULL DEFAULT 0,
    created_at  INTEGER NOT NULL,
    updated_at  INTEGER NOT NULL
);

-- 冻存盒（每层下的盒子）
CREATE TABLE storage_box (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    layer_id    INTEGER NOT NULL REFERENCES storage_layer(id) ON DELETE CASCADE,
    name        TEXT NOT NULL,          -- 盒子名称
    rows        INTEGER NOT NULL,      -- 行数（每个盒子独立配置）
    cols        INTEGER NOT NULL,      -- 列数
    note        TEXT,
    sort_order  INTEGER NOT NULL DEFAULT 0,
    created_at  INTEGER NOT NULL,
    updated_at  INTEGER NOT NULL
);

-- 样本位置（冻存盒中的每个格子）
CREATE TABLE sample_position (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    box_id      INTEGER NOT NULL REFERENCES storage_box(id) ON DELETE CASCADE,
    row         INTEGER NOT NULL,       -- 行号 (0-based)
    col         INTEGER NOT NULL,       -- 列号 (0-based)
    photo_path  TEXT,                   -- 照片文件路径 (内部存储)
    name        TEXT,                   -- 样本名称
    note        TEXT,                   -- 备注
    date        TEXT,                   -- 日期 (yyyy-MM-dd)
    created_at  INTEGER NOT NULL,
    updated_at  INTEGER NOT NULL,
    UNIQUE(box_id, row, col)           -- 一个位置只能有一个样本
);

-- 索引
CREATE INDEX idx_layer_device ON storage_layer(device_id);
CREATE INDEX idx_box_layer ON storage_box(layer_id);
CREATE INDEX idx_sample_box ON sample_position(box_id);
CREATE INDEX idx_sample_name ON sample_position(name);
```

### 核心 Entity 关系

```
StorageDevice 1 ──→ N StorageLayer 1 ──→ N StorageBox 1 ──→ N SamplePosition
```

---

## 3. 页面结构

### 页面清单（5 个主页面 + 3 个次级页面）

```
NavGraph:
├── DeviceListScreen        ← 首页：展示所有存储设备
├── DeviceDetailScreen      ← 设备详情：展示设备下的所有层
├── LayerDetailScreen       ← 层详情：展示层下的所有冻存盒
├── BoxGridScreen           ← 冻存盒网格：展示网格及每个位置的状态
├── SampleEditScreen        ← 样本编辑：编辑名称/备注/日期/拍照
└── SearchScreen            ← 搜索：全局搜索样本

次级页面（Dialog/BottomSheet）:
├── AddDeviceDialog
├── AddLayerDialog
├── AddBoxDialog
└── ExportBottomSheet       ← 选择导出范围和格式
```

### 每个页面的详细设计

#### ① DeviceListScreen（首页）
- Miuix TopAppBar — 标题 "LabFreezer"，右侧搜索图标
- 设备列表（Card 列表），每个卡片显示设备名、类型图标、层数统计
- 右下 FAB（+ 添加设备）
- 长按设备 → 编辑/删除
- 空状态：友好的引导图 + "添加第一个设备"按钮

**用户路径：** 打开 app → 看到所有设备 → 点击进入

#### ② DeviceDetailScreen
- Miuix TopAppBar — 返回 + 设备名称 + 更多菜单（编辑/删除/导出）
- 层列表（Card 列表），每层显示层名、盒子数量
- 右下 FAB（+ 添加层）
- 点击层 → 进入 LayerDetailScreen

#### ③ LayerDetailScreen
- TopAppBar — 返回 + 层名称 + 更多菜单
- 冻存盒列表（网格布局 Card），显示盒名、网格尺寸（如 "9×9"）
- 右下 FAB（+ 添加冻存盒）
- 点击盒子 → 进入 BoxGridScreen

#### ④ BoxGridScreen（核心页面）
- TopAppBar — 返回 + 盒名称
- 网格视图：N行×M列（根据 box.rows/box.cols 动态绘制）
- 每个格子状态通过颜色/图标区分：
  - 空白（灰色）
  - 已拍照（蓝色相机图标）
  - 已填写名称（绿色标签标识）
- 点击空格 → 直接打开相机拍照 → 保存后进入编辑
- 点击有内容的格子 → 进入 SampleEditScreen
- 长按格子 → 快速删除

**这是核心交互场景：** 在冷库前点击空格拍照，回办公室后点击编辑文字信息。

#### ⑤ SampleEditScreen
- TopAppBar — 返回 + 位置编号（如 "A1"）
- 照片预览（大图，可点击查看全屏）
- "重新拍照"按钮
- 样本名称输入框
- 日期选择器（默认今天）
- 备注输入框（多行）
- 保存按钮
- 删除按钮（带确认对话框）

#### ⑥ SearchScreen
- Miuix SearchBar 置顶
- 搜索结果列表，每项显示：
  - 样本名称 / 照片缩略图
  - 完整路径：设备名 > 层名 > 盒名 > 位置号
- 点击结果 → 跳转到 BoxGridScreen 并高亮对应位置
- 空搜索结果 → "未找到匹配样本"

#### ⑦ ExportBottomSheet
- 导出范围选择：全部 / 按设备 / 按层 / 按盒
- 格式选择：xlsx / Markdown / 同时导出
- "导出"按钮 → 生成文件 → 系统分享菜单

---

## 4. MVP 开发计划（6 个阶段）

### Phase 1：项目脚手架 + 数据库（1-2 天）
- [ ] 创建 Android 项目（Empty Compose Activity）
- [ ] 配置 Gradle（Hilt, Room, Compose, Miuix, Coil, CameraX, Navigation）
- [ ] 实现所有 Entity、DAO、RoomDatabase
- [ ] 实现 Miuix 主题配置（主题色、字体）
- [ ] 配置 Hilt DI 模块

**里程碑：** 项目可编译，数据库初始化成功

### Phase 2：设备管理 CRUD（1-2 天）
- [ ] DeviceListScreen + ViewModel
- [ ] DeviceDetailScreen + ViewModel
- [ ] LayerDetailScreen + ViewModel
- [ ] 添加/编辑/删除设备、层、冻存盒的 Dialog
- [ ] 导航图串联三个页面

**里程碑：** 可创建设备→层→盒子的完整层级

### Phase 3：冻存盒网格 + 相机（2-3 天）
- [ ] BoxGridScreen：动态网格绘制（根据 N×M）
- [ ] 网格位置状态显示（空白/已拍照/已命名）
- [ ] CameraX 拍照集成
- [ ] 拍照后照片保存到内部存储
- [ ] 照片缩略图加载（Coil）

**里程碑：** 点击格子→拍照→照片保存到该位置

### Phase 4：样本编辑（1-2 天）
- [ ] SampleEditScreen：名称、日期、备注
- [ ] 重新拍照
- [ ] 保存 / 删除功能
- [ ] 网格界面高亮显示已编辑状态

**里程碑：** 可以补全样本文字信息

### Phase 5：搜索（1 天）
- [ ] SearchScreen + ViewModel
- [ ] 全局模糊搜索（Room LIKE 查询）
- [ ] 搜索结果展示完整路径
- [ ] 点击跳转到对应 BoxGridScreen

**里程碑：** 可通过名称搜索到样本并定位

### Phase 6：导出（1-2 天）
- [ ] xlsx 导出引擎（Apache POI 或轻量方案）
- [ ] Markdown 导出引擎
- [ ] ExportBottomSheet（范围 + 格式选择）
- [ ] 系统分享菜单集成

**里程碑：** 可按范围导出 xlsx/Markdown 并分享

### 总计：约 7-12 天

---

## 5. Miuix 组件使用规划

| 原生 Material 组件 | 替换为 Miuix 组件 |
|---|---|
| MaterialTheme | MiuixTheme |
| TopAppBar | miuix TopAppBar |
| Navigation | NavigationBar (MIUI 底部栏) |
| Card/ListItem | miuix Card / ListItem |
| TextField | miuix TextField |
| SearchBar | miuix SearchBar |
| Dialog/BottomSheet | miuix Dialog / BottomSheet |
| FloatingActionButton | miuix FAB |
| Switch/Checkbox | miuix Switch / Checkbox |

---

## 6. 网格核心算法

### 位置编号规则
- 行用字母 A-Z 表示（A=第0行，B=第1行...）
- 列用数字 1-N 表示（1=第0列，2=第1列...）
- 例如：A1, A2, ..., B1, B2, ...

### 位置 ↔ 坐标 转换
```
fun positionToLabel(row: Int, col: Int): String = "${'A' + row}${col + 1}"

fun labelToPosition(label: String): Pair<Int, Int> {
    val row = label[0] - 'A'
    val col = label.drop(1).toInt() - 1
    return row to col
}
```

### 网格状态判断
```kotlin
enum class GridCellStatus {
    EMPTY,           // 未拍照、无数据
    PHOTO_ONLY,      // 已拍照、未编辑文字
    COMPLETE         // 已拍照、已编辑
}
```

---

## 7. 数据流示例：核心场景

**冷库前快速采集：**
```
用户在 BoxGridScreen
→ 点击空格 (row=2, col=5)
→ ViewModel 创建空的 SamplePosition
→ 触发 navigateToCamera(position)
→ CameraX 拍照
→ 保存照片到 {internal}/photos/{sampleId}.jpg
→ 回 BoxGridScreen，格子变蓝（已拍照状态）
```

**办公室后补充信息：**
```
用户回 BoxGridScreen
→ 点击蓝色格子
→ 进入 SampleEditScreen
→ 填写名称 "RNA-2024-001"
→ 选择日期 2024-03-15
→ 输入备注 "提取自HEK293T细胞"
→ 保存
→ 回 BoxGridScreen，格子变绿（完整状态）
```

---

## 8. 验证方案

每阶段验收标准：
1. **Phase 1**: `./gradlew build` 成功，数据库可创建
2. **Phase 2**: 可完成 设备 → 层 → 盒子 的全链路创建
3. **Phase 3**: 点击空格 → 拍照 → 照片显示在网格中
4. **Phase 4**: 可为样本填写文字信息并保存
5. **Phase 5**: 搜索关键词 → 显示结果 → 点击跳转定位
6. **Phase 6**: 导出 xlsx/MD → 系统分享 → 文件内容正确
